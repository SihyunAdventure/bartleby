# Vercel project root is configured as web/.
# Upload the web folder, but keep local Tauri build outputs, target/, node_modules/, and repo tooling out of CLI uploads.
/*
!/web
!/web/**
!/.vercelignore
# Vercel installs function deps from web/package.json itself — don't upload local node_modules.
/web/node_modules
